
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// This creates the React root and renders the App component
createRoot(document.getElementById("root")!).render(<App />);
